set -g default-terminal "tmux-256color"

set -g base-index 1
set -g pane-base-index 1
set -g prefix C-a
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g status-position top
set -g allow-rename off

unbind C-b
bind-key C-a send-prefix

unbind %
bind | split-window -h 

unbind '"'
bind - split-window -v

unbind r
bind r source-file ~/.tmux.conf

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind -r m resize-pane -Z

set -g mouse on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

# remove delay for exiting insert mode with ESC in Neovim
set -sg escape-time 0

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

# list of tmux plugins
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'alexwforsythe/tmux-which-key'

# --------------------tmux theme---------------------
set-window-option -g window-status-separator " "
set -g status-justify centre

# cold
# set-option -g status-style "fg=#d8d8d8"
# set -g status-left "#[fg=#383838,bold](#{?client_prefix,#[fg=#ac4242],#[fg=#d8d8d8,nobold]}tmux#[fg=#383838,bold])"
# set -g status-right "#[fg=#383838,bold](#[fg=#d8d8d8,nobold]󰆍 #S#[fg=#383838,bold])"
# setw -g window-status-current-format "#[fg=#383838,bold](#[fg=#f4bf75,bold]#I #[fg=#d8d8d8,bold]#W#[fg=#383838,bold])"
# setw -g window-status-format "#[fg=#383838,bold](#[fg=#808080,nobold]#I #W#[fg=#383838,bold])"

# catppuccin mocha
set-option -g status-style "fg=#cdd6f4"
set -g status-left "#[fg=#45475a,bold](#{?client_prefix,#[fg=#f38ba8],#[fg=#cdd6f4,nobold]}tmux#[fg=#45475a,bold])"
set -g status-right "#[fg=#45475a,bold](#[fg=#cdd6f4,nobold]󰆍 #S#[fg=#45475a,bold])"
setw -g window-status-current-format "#[fg=#45475a,bold](#[fg=#f9e2af,bold]#I #[fg=#cdd6f4,bold]#W#[fg=#45475a,bold])"
setw -g window-status-format "#[fg=#45475a,bold](#[fg=#9399b2,nobold]#I #W#[fg=#45475a,bold])"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
