local colors = require("colors")

sbar.bar({
  height = 32,
  color = colors.transparent,
  position = "top",
  sticky = true,
  corner_radius = 12,
  padding_left = 0,
  padding_right = 0,
  y_offset = 3,
  margin = 6,
  blur_radius = 33,
})
